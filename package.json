{
  "name": "wireguard-vpn",
  "version": "0.0.1",
  "author": "Vladislav Grigoriev",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "client:dev": "yarn workspace @wireguard-vpn/client dev",
    "server:dev": "yarn workspace @wireguard-vpn/server start:dev",
    "build:packages": "yarn workspace @wireguard-vpn/common build",
    "prepare": "husky"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.2"
  }
}
